import NetworkCartographer from "./NetworkCartographer";

export const metadata = {
  title: "Tools — CARTOGRAPHER | Movementum",
  description:
    "Drop your LinkedIn connections export. See where your network is dense and find the companies where you already know someone. Local-only, nothing uploaded.",
};

export default function NetworkCartographerPage() {
  return <NetworkCartographer />;
}
